version: 0.2
phases:
  install:
    runtime-versions:
        nodejs: 12
  build:
    commands:
      - sam package --template-file template.yaml --s3-bucket lijing-test-be --s3-prefix be --region eu-central-1 --output-template-file packaged-template.yaml
artifacts:
  files:
    - packaged-template.yml